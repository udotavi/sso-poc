// src/AdminComponent.js

import React from "react";

const AdminComponent = () => {
  return (
    <div style={{ color: "red" }}>Only admins should see this section.</div>
  );
};

export default AdminComponent;
